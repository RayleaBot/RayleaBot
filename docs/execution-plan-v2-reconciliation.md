# v2 补充评审核实与收敛

本轮入口为用户列出的十组残留及新增问题，基线为 `f782207d`。此前完整 CI 和候选产物验收只证明对应检查已通过，不能据此认定全部重构目标已满足。本轮按源码、契约和真实消费者逐项核实，C01–C12 已完成收敛；行数和出现次数用于定位，不独立作为通过标准。

实施与验证使用隔离测试数据，沿用冻结工具链版本线；实际证据与未执行范围记录在本台账。

| ID | 收敛范围 | 状态 | 核实、实施与验证 |
| --- | --- | --- | --- |
| C01 | 错误码目录、HTTP 适用范围、ReasonCodes 与 SDK 检查 | 完成 | `823d80a4`：登记恢复诊断及 SDK 本地 shutdown 取消码，修正 3 个非 HTTP 码的状态；生成器/契约校验双向约束 HTTP 范围，架构检查扫描 ReasonCodes、直接赋值、append 与 SDK。`197f2234` 同步修正三个 404 接口描述的代码名称；严格契约、生成器/校验器单测、架构及 readiness 测试通过。 |
| C02 | 适配器鉴权、WS 插件状态、插件公共 schema | 完成 | `272af5ca`：补齐 `/api/adapters` 鉴权；WS 复用 HTTP 状态/诊断/命令，静态声明字段复用 plugin-info 定义。缺项实际为诊断 kind 的 initialization_failed。增加 fixture、保护路由对照及跨文件 schema 校验；严格契约、Web 生成/typecheck、Launcher 子集校验通过。 |
| C03 | P16 传递依赖、SQLite 实现归属、Ingress 接口、runtime 协议依赖 | 完成 | `c2a52049`：权限、日志、凭据 SQLite 实现移入各自 sqlite 包；菜单渲染使用消费方回调、配置更新使用 config 接口，身份投影与策略事件为轻量模型。Ingress 目录/事件桥均使用实际接口；runtime 动作分类由契约生成。go list -deps 已证实五个目标包均不含 storage/sqlcgen/render；架构门禁、相关包、Server 集成与全包编译通过。 |
| C04 | 路径包含、摘要原语与下载包装 | 完成 | `48659dd4` / `5d93b645`：渲染、静态资源及安装统一词法包含判断；保留路径名、链接/reparse 策略。文件/目录摘要集中到 fsguard，保持目录摘要格式并传递安装取消信号，文件关闭失败传播；移除下载进度/来源/锁/摘要的转发包装。摘要固定向量、大小与取消边界、安装、更新、运行资源、管理及渲染资源测试通过。 |
| C05 | 更新服务缺失、治理分支、凭据约束、HTTP 重试默认值、artifact 标识 | 完成 | `ef78b623`：更新路由装配拒绝缺少服务，凭据长度校验归认证领域，HTTP 重试与超时默认值来自配置 schema，黑名单移除白名单专属例外。`99004a78` 从发布契约生成 artifact 标识并接入 Server、Launcher 与打包脚本。相关服务、HTTP 集成、元数据及契约检查通过。 |
| C06 | 日志工作区与黑白名单组件 | 完成 | `3c834add`：两日志页共享完整工作区和控制器，筛选/URL、详情、分页、重试、KeepAlive 与虚拟视口生命周期只有一份；实时追新与历史时间窗保留各自数据状态。黑白名单共享表格和编辑模板，保留独立草稿、筛选与错误；修复复制失败反馈、详情失败 URL、旧详情残留和跨页签回写。相关 31 个组件用例及最终 Web 全量 466 项通过。 |
| C07 | i18n、时间/色调、HTTP/WS 类型与断点 | 完成 | `3c834add`：目标页面、基础控件及连接错误统一 locale；三个消费者共用时间归一化与状态色调。HTTP 静态路径受 OpenAPI 约束，33 个动态请求使用模板构造与参数编码，编译负例拒绝未登记子路径、缺参数及裸动态 URL；WS 路径/事件由契约生成。断点由设计 token 生成 Sass/TS，实际 CSS 中无媒体查询变量残留。三个 bridge payload 类型用于真实发送数据约束。生成、类型、构建及全量单测通过。 |
| C08 | mock 后端与 production 测试边界 | 完成 | `d09e5a75`：生产配置只运行真实 Server；凭据、治理、密钥遮罩、时区生效和实际插件 iframe 链路移入隔离运行目录。mock 使用契约样例及显式响应快照，移除秘密合并、治理去重、安装/来源规则、真实游标副本和闲置静态托管；JSON 错误受正式目录约束。最终 10 个真实 Server 与 110 个受控浏览器用例通过；最后错误目录收口的 7 个浏览器场景、4 种门禁负例及正常 JSON 保真检查通过，测试进程均退出。 |
| C09 | Launcher 生成模型、展示适配与更新平台分支 | 完成 | `15e1b33b`：Go 本机模型定义 8 组枚举并生成 Wails 绑定，前端由绑定派生，适配只处理空数组、生成枚举和正式 HTTP 投影；65 个前端单测、Go 测试及类型检查通过。平台问题部分不成立：Linux/macOS 在调用助手前返回引导更新，解析函数明确命名 Windows 边界，跨平台用例保留。artifact 标识由 `99004a78` 接入契约生成链。 |
| C10 | 工具链版本源、归档禁止策略与签名失败 | 完成 | `5ca2cccd`：工具链版本源接入 CI/nightly/发布、doctor、契约校验及开发工作区，冻结值不变；Bash/Node/Python 读取传播、换行和非法声明用例通过。`6e3e0d02` 统一内容规则，并将已配置或预签名产物的签名失败设为构建失败，10 种签名门禁场景通过；`e133093e` 明确主程序发布者证书与第三方依赖证书的职责。核实两份归档规则集合原本等价，修复的是重复维护入口。`99004a78` 完成 artifact 标识生成；`e7e15d48` 将开发容器安装版本接入同源读取，并校验基础镜像声明。 |
| C11 | testCatalog、副本业务、无断言 race、live smoke、文案断言 | 完成 | `a68cda1d`：管理/安装使用真实 Catalog，删除两个业务替身；并发用例断言更新、成员、快照和最终状态并通过 race。错误与事件使用稳定结构字段；名称、输入、语法、脱敏及专门格式化断言保留。QQ live 原本是缺凭据时跳过，改为已登记的 manual_smoke 显式用例，缺输入即失败；仅做编译检查，本轮未连接真实 QQ 或发送消息。 |
| C12 | 安装/控制/恢复复杂度、测试专用导出、空目录、零消费生成类型 | 完成 | `aa6bb940`：恢复主流程复杂度 115→19、归档检查 64→7，故障注入、CRC、数据库锁和 Windows junction 用例通过。`548f53cd` / `8d771bbe`：安装主流程 85→7、来源准备 60→3、解压 47→21；Controller 按实际职责分文件，共享期望状态写入与任务指针助手。安装/controller 原文件现为 390/332 行；完整生命周期 race、真实安装集成和 Server 全量测试通过。无消费者管理导出已由 `ef78b623` 删除，两处确认空目录已删除；零消费生成 payload 已由 `3c834add` 接入实际发送消费者。 |

完成条件：每条用户发现都有核实结论、处置与证据；确认的缺陷已修复并覆盖关键失败路径；正式契约、生成物与消费者一致；原计划中高报的状态和说明已纠正；未执行范围明确记录。

## 汇总证据与边界

- Server 全量 `go test ./...`、独立 Go SDK 全量通过；安装/恢复故障路径和真实安装集成通过，Catalog 与完整 Lifecycle race 通过。最终核心 lint 为 0 issues，五个目标包的传递依赖再次检查无 storage/sqlcgen/render。
- Web 最终 81 个文件、466 个单元用例、10 个真实 Server 及 110 个受控浏览器用例通过，类型检查和构建通过；Launcher 65 个前端用例、类型检查及 Go 测试通过。Web 生成物与 OpenAPI/WS 生成器输出一致，设计生成检查通过；实际 CSS 的媒体条件使用尺寸实值。文档链接与 AGENTS/CLAUDE 结构检查通过。
- 严格契约、错误目录/协议/Launcher/运行 schema 生成检查通过。发布 Python 用例 109 项通过；工程 Python 55 项中 50 项通过，5 项 POSIX 启动脚本用例因 Windows 平台跳过；工程 Node 78 项通过。开发容器的版本读取、基础镜像漂移与 shell 语法已检查，本机没有 Docker，未构建容器镜像。
- Linux amd64 与 macOS arm64 的 Server 交叉构建通过，检查了 ELF/Mach-O 产物。交叉编译不代表目标平台原生运行或桌面界面验收。
- 本轮未连接真实 QQ 或执行其他外部平台登录/消息发送；`manual_smoke` 显式入口与所需输入见[人工 Smoke](engineering/manual-smoke.md)。本轮也未重新生成正式分发包、执行正式签名或发布。原 P26 的 `5b2fa9d8` 候选证据保留为历史记录；分发本轮新源码前需按新提交重新执行对应产物验收。

## 保留的实现与说明

management 保持一个 HTTP 装配包。本轮已移出凭据规则、去除伪造的更新状态和测试专用导出；其余 handler 的文件数不独立构成拆包依据。生产测试中用于协议格式、用户输入、名称、脱敏与格式化的中文断言继续保留；错误分类和事件选择改用稳定结构字段，不以中文字符出现次数判断测试质量。

模拟日志页只提供受控展示数据和夹具游标，模拟会话只允许测试页面经登录流程进入；它们不证明 Server 的持久化、鉴权或排序策略。真实 Server 的浏览器覆盖及运行入口见[Web 端到端验证](engineering/web-testing.md)。
