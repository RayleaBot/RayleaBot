# Web UI 迁移实施记录

执行顺序与验收条件见[迁移计划 v1](../execution-plan-v1.md)。本页记录实际实现和测量，不替代当前工程基线。

## M0 技术验证

日期：2026-09-07。功能对照提交：`612c5bc3`。

| 项目 | 结果 |
| --- | --- |
| Reka UI | `2.10.4`，Vue peer 范围覆盖项目 Vue `3.5.41` |
| Motion for Vue | `2.4.2`，使用 `@vueuse/core 14.4.0` |
| shadcn-vue CLI | `2.8.2`，`reka-nova` registry；源码摘要随组件记录 |
| 通用图标 | registry 当前采用 `@lucide/vue 1.41.0`；迁移期保留未迁移页面的旧图标 |
| 组件工具 | `class-variance-authority 0.7.1`、`clsx 2.1.1`、`tailwind-merge 3.6.0`，用于已导入组件的变体与类名合并 |
| 隔离原型 | Vue `3.5.41`、Vite `8.2.1`、Tailwind `4.3.3` 下构建与浏览器交互通过；应用工程的完整类型检查见下文 |
| 原型交互 | Reka Dialog 与 Motion 的 Portal、进入与退出；嵌套 Select 选择；Escape 关闭；焦点归还通过浏览器验证 |
| 构建脚本 | 已审阅 `vue-demi 0.14.10` 安装脚本，仅选择本地 Vue 3 导出文件；在当前 Web 工作区允许该脚本 |
| 导入验证 | 修正引用工程检查入口后，Web 应用、构建配置与浏览器测试代码的完整类型检查通过 |

脚手架生成结果曾包含外部字体导入与默认浮层位移动画。迁移仅导入审阅后的组件源码，不复制它生成的全局 CSS、字体或主题预设。

Motion for Vue 内部使用 Motion/Framer 的 DOM 能力；Vue 管理面没有因此引入第二个 React 应用。依赖包和实际输出资源仍需在最终构建阶段复核。

## 旧版资源与交互对照

测量对象为基准提交的生产构建，使用本机 Chromium、`1440×960` 视口和隔离 mock 后端；通过正式登录接口建立会话。下列数据是一次本机观测，不是跨设备性能承诺。

| 指标 | 基准值 |
| --- | --- |
| 全部 JavaScript 产物 | 44 个文件，1,897,210 bytes；逐文件 gzip 合计 593,773 bytes |
| 全部 CSS 产物 | 31 个文件，340,212 bytes；逐文件 gzip 合计 94,492 bytes |
| 协议中心当次加载的 JS/CSS | 20 个资源，解码体积 1,623,587 bytes；不包含字体与图片 |
| 页面长任务 | 当次记录到 1 个超过 50ms 的任务，持续约 153ms |
| 已修复的旧版弹窗 | 采集到 30 帧，中心偏移最大约 0.008 CSS px |

试点与最终方案应沿用同一数据量、视口和采样方式。迁移期新旧组件共存会影响总资源体积，最终清理后再核对完整产物；中途也应检查试点实际加载资源与操作响应。

## 当前实施状态

- 已建立独立迁移分支和工作区，主工作区未切换技术栈。
- 已锁定核心依赖并引入基础组件源码，项目字体和共享 token 来源保持独立。
- 协议中心内部已使用新产品组件，保留连接卡片、渐进式表单、草稿保护、正式配置合并及深链。
- 应用壳与认证入口已迁移，新增抽屉、菜单、页签、分段选择和提示宿主；其他业务工作区按 M4 分批迁移。
- M4-A 的插件列表、商店、详情、全局插件设置、菜单中心和指令中心已迁移；M4-B/C 的账号、治理与诊断页面仍保留其旧控件。

## M1 / M2 组件和交互

产品组件集中负责按钮变体、字段标签与错误关联、字符串与数值输入、密码可见性、单选与多选、复选框、开关、状态反馈、弹窗和确认。组件展示页位于开发构建的 `/__dev/components`，要求管理会话，生产产物不包含该入口。

弹窗位置由 CSS 固定在视口中心。Motion 以 220ms 播放透明度和 `0.96 → 1` 缩放，以 160ms 退出；内容高度通过实际测量插值，较短视口由正文区域滚动。减少动态效果和强制配色环境使用即时状态变化。嵌套弹窗与选择器通过共享浮层顺序排列，主按钮前景使用共享 `on-brand` token。

Reka 的交互节点保持挂载，直到 Motion 完成退出后才释放遮罩、滚动锁和焦点锁。仅在 Reka 外包裹 AnimatePresence 会出现“画面还在退出、交互已卸载”的问题；当前产品封装由 Motion 完成事件控制 `active` 状态。兼容矩阵等业务内容也由这一层统一控制挂载。

### 验证结果

| 检查 | 本轮结果 |
| --- | --- |
| 类型与生产构建 | `pnpm run typecheck`、`pnpm build` 通过，已确认生产代码不包含组件展示入口 |
| 协议与关联业务单元测试 | 7 个文件、32 项通过，覆盖草稿、合并、冲突、失败保留、多传输、适配器快照路由与主题映射 |
| 浏览器路径 | 10 个不同用例通过：5 项既有协议/跨页路径、5 项新组件/动效路径 |
| 实际动画中间帧 | 进入、退出和内容高度变化时中心偏差不超过 1 CSS px；退出期间焦点仍在弹窗内 |
| 退出内容保留 | 兼容矩阵在中间帧保留全部行，完成后卸载并归还焦点 |
| 可操作性 | QQ 事件多选、嵌套取消、快速前进后退、`390×460` 低高度视口、减少动态效果与强制配色通过 |
| 视觉评审 | `1440×960` 桌面与 `390×844` 窄屏、明暗主题及嵌套确认已检查；独立评审要求的矩阵提前卸载问题修复后，复核判定该项已解决 |
| 发布归属 | 生成脚本纳入自有组件源码的上游 MIT 版权文本；10 项许可证脚本测试通过 |
| 工程说明 | 缩进、文档链接和 agent-doc 检查通过 |

这些检查使用隔离 mock 后端和正式登录流程，没有连接用户的真实配置、数据库或机器人。尚未执行迁移后的全工作区验收、真实插件 iframe 回归及完整发布启动检查。

### 类型检查入口修正

原命令 `vue-tsc --noEmit` 只读取顶层空 `files` 配置，没有遍历应用和 Node 引用工程。入口现为 `vue-tsc --build`，同时补齐测试工程的浏览器类型环境。由此发现的适配器类型别名缺失、既有 WebSocket 适配器快照分支未生成和旧 Antd token 名称错误已修复。

正式日志筛选枚举目前仅包含 OneBot11。QQ 连接的诊断入口进入实时日志，不发送契约外的协议筛选值。

### 首批资源体积

| 指标 | 基准提交 | M1 / M2 生产构建 |
| --- | --- | --- |
| JavaScript 文件数 | 44 | 46 |
| JavaScript 原始 / gzip 合计 | 1,897,210 / 593,773 bytes | 2,171,612 / 680,350 bytes |
| CSS 文件数 | 31 | 30 |
| CSS 原始 / gzip 合计 | 340,212 / 94,492 bytes | 384,319 / 102,288 bytes |

本表是全部产物的逐文件大小及 gzip 合计，不能等同于某个路由的首屏传输量。首批同时保留旧组件和新基础设施；最终清理后再对照加载资源、主线程长任务及滚动场景。

## M3 应用壳与认证入口

导航、插件子导航、工作区页签及右键菜单、搜索、设置抽屉、主题菜单和登录表单已使用产品控件。原工作区身份、缓存与恢复、插件管理深链、正式登录和错误处理仍由既有模块负责。页面内的旧组件暂时继续使用顶层 Antd 主题 provider。

左右抽屉复用 AppDialog 的焦点与退出生命周期，以固定视口高度滑入；居中弹窗单独对内容高度插值。不能给居中模式绑定空的内联高度，否则 Vue 更新会打断 Motion 的插值。菜单触发抽屉后，离场菜单位于新浮层下方；触发项卸载时，关闭操作回到仍有效的菜单按钮。

登录保留青瓷玻璃材质，账号与密码使用相同的 50px 输入框、错误关联和焦点样式。密码可见性按钮支持键盘与忙碌禁用。全局提示由 Reka Toast 负责时限和可访问性，保留原业务反馈函数；同时最多显示四条，错误提示留置更久，并提供关闭操作。

已通过完整类型检查、生产构建，以及登录表单、认证主题和导航动效的 12 项单元测试。18 项浏览器回归在同一批次通过，包含协议浮层回归、偏好持久化、菜单到抽屉的焦点交接、键盘搜索、折叠导航、插件筛选与展开、页签关闭、认证和 `360/768/1280px` 视口。

独立视觉评审发现上游 Tooltip 的 `max-w-xs` 与项目 `--spacing-xs` 映射冲突，导致提示逐字换行。组件已改为明确的 `min(20rem, 视口宽度 - 24px)` 上限；新增搜索、全屏和设置三种提示的浏览器回归通过，本阶段共覆盖 19 项浏览器用例。六张同路径截图已重截；评审复核将宽度问题和 M3 文档交接均判定为已解决，结论为 `ship`，范围限于这两项发现。

## M4-A 插件工作区

插件集合保留独立对象卡片、状态和操作；详情保留元数据、指令、控制台与独立管理页面。全局插件设置仍使用原配置草稿和底部保存栏，菜单中心仍由原模板与原生预览 iframe 呈现。指令中心改用原生语义表格，宽表只在自己的滚动区内展开。

本批补齐标签输入、多行文本、定义列表、显式搜索和表格组件。选择器在产品边界保留字符串、数字和布尔值，未出现在当前选项中的筛选值仍可显示与清除。泛型布尔属性显式保留运行时 Boolean 类型，避免裸 `multiple` 或 `nullable` 属性被视为空字符串。

移动筛选采用贴底抽屉。安装检查、商店核验与取消流程保留全部正式内容，退出后才清理检查结果；插件源删除使用嵌套居中确认。常驻页签保留预览 iframe 的文档，插件管理宿主继续维护自己的跨源握手、主题、设置与密钥通信。

| 检查 | 结果 |
| --- | --- |
| 插件相关单元测试 | 8 个文件、38 项通过 |
| 协议、认证与导航基础回归 | 4 个文件、25 项通过 |
| 插件浏览器回归 | 13 项通过，包含安装核验、取消不写入、源增删与取消、配置保存、控制台恢复、筛选值类型及字面逗号、未知筛选清除、预览文档保留和跨源插件 bridge |
| 协议与外壳浏览器回归 | 9 项通过，覆盖逐帧居中、退出焦点锁、嵌套选择、偏好、搜索和工具提示 |
| 类型与产物 | 完整 `vue-tsc --build` 和生产构建通过 |
| 工程检查 | 缩进、设计 token、颜色漂移与 diff 检查通过 |
| 视觉评审 | 8 张桌面、移动与暗色截图有效；独立评审结论为本批 `ship`，没有要求修复的实质问题 |

隔离 iframe 验证先按锁文件安装 `sdk/vue` 的依赖，再安装并构建 `examples/plugins/example-config-panel/ui`。这些步骤只产生未跟踪的依赖和构建目录，没有修改 SDK、示例插件或 Launcher 源码。测试通过正式登录接口建立 mock 会话；本批没有连接真实第三方账号或机器人。

检测器提示原有 iframe 高度 CSS 过渡会改变布局。评审结合既有宿主尺寸同步和全局减少动态效果规则，没有将其判为此次迁移缺陷；本批保留该实现，最终性能对照仍在 M5 执行。

## M4-B 账号、治理与配置

三方账号保留独立账号卡、行内草稿、凭据留空保留语义、扫码会话和服务器校验状态。名单保持完整列表与行内新增，宽表在自己的滚动区域内展示；删除和空白名单启用使用居中确认。权限、限流和通用配置沿用现有配置草稿、保存与生效范围反馈，布尔选项和清空数值继续保留原始类型语义。

共享选择框直接从当前选项解析显示名称，避免异步加载后仍缓存未知 ID。弹窗在打开时保存备用焦点位置，使删除对象在退出完成后清理时，仍能回到有效入口。名单字段的错误状态与提示通过 ARIA 关联，复制图标预留空间，以透明度反馈替代宽度动画。

二维码使用 [qrcode-generator](https://github.com/kazuhikoarase/qrcode-generator) `2.0.4`，它只承担编码，不引入另一套 UI 组件。编码入口显式转换 UTF-8，保留四模块静区和适合扫码的固定图像颜色；失效、扫码完成、错误与刷新状态由 `AppQRCode` 呈现。包内仅有源码头部版权声明，因此版本化许可证补充文件保留作者与 MIT 文本，并由发布生成脚本收集。

| 检查 | 结果 |
| --- | --- |
| 账号、治理与配置浏览器回归 | 13 个不同用例通过，覆盖四个平台扫码、临时轮询失败、浏览器验证与放弃会话、凭据留空保存、名单增删与空白名单确认、权限和配置保存、异步选项名称、删除取消与成功后的焦点 |
| 配置与治理单元测试 | 5 个文件、25 项通过，包含布尔选项与清空数值语义 |
| 共享浮层和应用壳回归 | 9 项通过，包含实际动画中间帧、退出内容、焦点锁与嵌套关闭 |
| 二维码编码与渲染 | 独立解码器还原普通链接和含中文、表情的链接；浏览器实际渲染的 GIF 也成功还原输入链接 |
| 类型与产物 | 完整类型检查与生产构建通过 |
| 发布归属 | 11 项许可证脚本测试通过，二维码作者版权与 MIT 文本进入 notices |
| 视觉记录 | 9 张桌面、窄屏、暗色、扫码与居中确认截图有效；独立完整评审结论为本批 `ship`，无实质修复项 |

验证使用隔离 mock 会话及正式登录接口，未登录真实第三方账号。诊断页面迁移和最终性能、全量验证仍在后续阶段执行。
