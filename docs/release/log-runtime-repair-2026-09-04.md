# 近期日志与运行故障修复验收

日期：2026-09-04。调查窗口为 8 月 28 日至 9 月 4 日，调查基线包含 48,404 条管理日志。

修复范围为 RayleaBot 主仓库与独立的 `plugin-subscription-hub`。另外五个插件只通过正式开发入口重建并同步本地 SDK，源码仓库保持干净；没有外部发布或 Git 提交。

## 根因与修改对应

| 问题 | 根因 | 修复与证据入口 |
| --- | --- | --- |
| 事件响应关联被覆盖 | 请求 ID 仅依赖时间戳，重复注册可覆盖已有会话 | [Runtime](../../server/internal/plugins/runtime/manager.go) 使用进程随机前缀和原子序号；事件、初始化和心跳统一生成 ID，重复注册拒绝，完成时校验会话对象及进程实例 |
| 并发启停、重载产生状态竞争 | 启动缺少完整占位，旧进程回调可能作用于新实例 | [生命周期协调](../../server/internal/plugins/lifecycle/controller.go) 按插件串行协调启停重载，在线快速路径也持锁；退出回调与退避重启校验当前实例 |
| 正常取消被误报事件超时 | 异步队列保留短生命周期调用者 context | [分发入口](../../server/internal/eventpipeline/dispatch/delivery.go) 入队前检查取消，接收后使用 Dispatcher 插槽生命周期；同步管理请求仍遵守调用方取消 |
| SDK 迟到响应或提前终态触发协议错误 | 调用方停止等待被当成宿主动作结束 | [SDK 动作生命周期](../../sdk/go/event_actions.go) 保留已发送动作关联，关闭事件后禁止新动作；终态等待结算，最长使用现有 ActionTimeout；未结算则不发非法终态，迟到响应有界保留，真正未知帧仍拒绝 |
| 停机时任务历史缺失、同一故障多层告警 | 执行与收尾共用取消上下文，责任层不清 | [调度收尾](../../server/internal/eventpipeline/dispatch/scheduler_log.go) 使用独立 5 秒预算；[关闭顺序](../../server/internal/app/app_run.go) 在关闭数据库前停止调度并排空分发；一次运行结果只记录一次 |
| 订阅检查超出宿主时限、检查重叠 | 随机等待、锁等待和执行预算叠加 | 独立插件 `internal/plugin/check_pipeline.go`：入口总预算 50 秒，最后 5 秒用于收尾；定时检查不重叠，手动等待可取消；任务注册单次并发执行且失败可重试 |
| 冷却倒计时反复成为新错误 | 冷却状态混入本轮请求失败，日志按整轮而非原因节流 | 独立插件 `subscription_check.go`、`check_failures.go`：按来源、阶段、错误码及机器原因聚合；新类别首条立即可见，同类每 5 分钟汇总，确认恢复后记录次数；文案不参与聚合键 |
| 发送回执超时被误称确定失败 | 请求已发出，但缺失确定回执 | [OneBot 出站](../../server/internal/onebot11/shell_outbound.go) 使用 `adapter.send_unconfirmed`；调用前已取消不会发送，明确拒绝仍使用已有错误码，结果未知不自动重试 |
| 媒体文件提前释放、未知结果重复发送 | 清理绑定函数返回，而非发送结算 | 独立插件 `media_retention.go`、`deferred_media.go`：原路径保留 24 小时，租约持久化，仍占 8 个任务配额；重启读取保留记录但不重放；仅明确未发送的限流拒绝允许重试，部分批次不整批重发 |
| 普通订阅未知结果在下轮重发 | 本地未收到成功结果便没有去重记录 | 发送前保存现有 seen 去重保护；未知结果保留，明确未发送才移除；不在事件结束后追加宿主通知 |
| 重复字段和启动日志镜像膨胀 | 规范字段与同值镜像重复保存，子进程全文又写入启动日志 | [日志规范化](../../server/internal/logging/details.go) 去除同值镜像，逐条收发保留；相同 `raw_message` 省略，不同内容仍保留；[开发启动器](../../scripts/start-dev.mjs) 只在启动日志记录编排、命令与子日志位置 |
| setup 凭据落盘 | 非交互 stderr 仍输出首次设置地址 | [Server 入口](../../server/cmd/raylea-server/main.go) 仅在确需初始化且连接真实终端时显示；完整行跨块解码后脱敏，覆盖 stderr、管理详情及 JSON 文本前缀；媒体 URL 的访问密钥也纳入今后脱敏 |

请求 ID 覆盖是已确认的源码缺陷，但不能据此断言每条历史协议错误都由它引起。历史编译错误与已经正确实现的 `render_footer` 问题没有重复修改。

未扩大队列，未提高宿主全局事件超时，未通过吞掉未知协议帧隐藏问题。

## 契约与用户可见语义

- [错误码目录](../../contracts/error-codes.yaml) 新增 `plugin.event_canceled`，取消不再归为 `plugin.event_timeout`，调度使用已有 `other` 分类，没有新增统计列。
- 新增 `adapter.send_unconfirmed`：请求已发出但结果未确认，可能仍会送达，禁止自动重试；不同于已经确定的发送失败。
- [插件协议](../../contracts/plugin-protocol.schema.json) 保持版本 2；[日志 API](../../contracts/web-api.openapi.yaml) 顶层结构不变，原因、关联和重复次数使用结构化详情。
- 错误说明、fixtures、Web 映射、Web/Launcher 生成类型及 SDK 文档同步更新。
- 日志区分插件内部事件未完成、任务未执行、消息发送失败及发送结果未确认。冷却期间仍能在手动检查结果中看到原因与预计重试时间。

## 历史数据处理

在没有日志写入进程时，对 40 个日志文件中的 2,917 处 setup URL 凭据进行了精确替换，保留 442,338 行、文件时间和其他字节；再次扫描为 0 处匹配。

未删除历史，未清空数据库，未重置调度统计，也未复制新的明文备份。管理数据库未检出 setup 地址，因此未作无依据的数据改写。工具与操作边界见[日志说明](../dev/logging.md)。

这不承诺抹除磁盘历史扇区、外部备份或操作系统快照，也不表示所有历史媒体链接都被重新处理。

## 验证结果

| 验证面 | 结果 |
| --- | --- |
| Server | 最终 `go test -p 1 -count=1 ./...`、`go vet ./...` 通过，包含 integration、services、ws、architecture |
| 高风险回归 | 请求 ID、重复注册、并发启动、取消分类与调度收尾关键回归连续重复 20 次通过；生命周期等待取消和现有重载测试通过 |
| SDK | 测试、vet 通过；覆盖取消后的迟到响应、未结算动作阻止终态、终态后的动作拒绝、关闭取消、长等待释放及未知响应拒绝 |
| 独立插件 | 6 个 SDK 消费插件测试、vet 通过；订阅插件新增预算、互斥、冷却、新故障独立记录、汇总计数、延迟媒体读取、限流重试、未知结果保留与重启不重发回归 |
| Web | 63 个测试文件、336 项测试通过；类型检查与构建通过 |
| Launcher | 类型检查、平台 vet 与开发构建通过；API 生成说明同步 |
| 脚本与契约 | 52 项 Node 脚本测试通过；contract self-test、strict、runtime schema 一致性验证通过 |
| 静态检查 | 受影响 Go 包的改动行 golangci-lint 为 0 issues；两个源码仓库的 diff 检查通过 |
| 跨平台 | Linux amd64、macOS arm64 Server 交叉构建通过，产物已核对；不是对应平台的运行验收 |

验证限制：

- 本机 Windows 的 `CGO_ENABLED=0`，没有可用 C 编译器或 WSL，未运行 `-race`。确定性并发测试不能替代 race 验证。
- 初次默认并行全量运行曾出现子进程关闭超时；独立包及串行全量复跑通过。没有调高测试或运行时超时来消除失败。
- 日志字段去重后，旧集成 fixture 曾要求重复的 `raw_message`；已同步契约说明与 fixture，最终全量复跑通过。
- 全仓已有未改代码仍有 errcheck 告警，不能将“改动行静态检查通过”表述为“整个仓库 lint 清零”。
- 24 小时媒体保留清理使用测试时钟验证，未等待真实 24 小时；现场没有人为制造群聊消息或上游回执超时。
- 部分集成测试退出后遗留托管 Chromium 实例。本轮按进程路径、创建时间和已退出的测试父进程确认后，停止了 12 个测试浏览器进程树，未改动运行服务或无法确认归属的实例；测试夹具的浏览器释放问题仍需另行定位，不能据此推断历史窗口错误的根因。

## 最终运行验收

北京时间 **13:57:47–14:27:48**，观察 1,800 秒后正常关闭：

- 30 次健康检查均为 HTTP 200 / ready。
- 140 条 INFO、5 条 WARN，0 条 ERROR；5 条 WARN 均为 `douyin:source:feed:session_blocked`。
- Runtime、分发、调度层新增故障记录为 0；没有新增事件超时或协议违规告警。
- 79 条自然入站、1 条带回执消息 ID 的成功出站；数据库对应记录分别为 79 与 1，数量一致。
- 入站日志中的丢弃数、失败数均为 0；没有相同正文的重复 `raw_message` 字段。
- 关闭接口返回 202，Server 退出码 0，没有强制终止；数据库 `quick_check` 为 `ok`。

| 定时任务 | 成功数：前 → 后 | 失败数：前 → 后 | 超时数：前 → 后 | other：前 → 后 |
| --- | --- | --- | --- | --- |
| subscription-hub-check | 36,659 → 36,689（+30） | 759 → 759 | 439 → 439 | 0 → 0 |
| subscription-hub-media | 2,010 → 2,041（+31） | 705 → 705 | 9 → 9 | 0 → 0 |

历史 `last_error_code` 仍可显示 `plugin.event_timeout`，这是保留的历史诊断字段，不代表最终观察期间产生了新超时。

调度成功表示插件事件按协议完成，不表示每个上游来源都成功拉取。HTTP 403 仍在订阅检查结果中记为降级，未被计作宿主事件超时或隐藏。

验收摘要保存在本地 `logs/dev/validation/2026-09-04T05-57-46-866Z.json`，明细位于同名 `.log`。此前还有一次完整 30 分钟观察通过；中途用于补充复核的短观察被主动停止，不计入最终验收。

### 降噪前后

| 类别 | 调查窗口中的问题 | 最终观察 |
| --- | --- | --- |
| 插件事件与调度 | 临时 context 取消误报超时，协议故障跨层重复告警 | 新增 Runtime / 分发 / 调度故障 0；统计仍正常累加 |
| 冷却与轮询 | 无变化轮询和冷却倒计时持续占用告警日志 | 冷却不再计为新请求失败，正常轮询 DEBUG；真实接口拒绝保留 5 条 |
| 消息记录 | 规范字段与同值镜像重复 | 79 入站 / 1 出站均保留并落库，同值正文镜像为 0 |
| 启动日志 | 保存子进程全文，与子日志重复 | 构建窗口抽查 55 条启动记录无 Server 原始 JSON 帧；只记录编排、命令和日志位置 |

调查窗口和最终观察的时长、消息负载不同，因此不把总条数直接相除，声称一个未经归一化的“降噪百分比”。

## 开发产物与残余问题

通过 `scripts/start-dev.mjs` 的正式构建和同步流程更新 Server 与六个插件，安装文件与构建输出的 SHA-256 全部一致，未手改 installed 副本。插件 Go 元数据确认使用本地 workspace SDK；独立仓库没有伪造新的 SDK 发布版本。

验收后已恢复原有 `web-dev`、Server `watch` 和插件 `watch` 模式。Launcher 与 Web 开发服务正常启动，管理接口返回 HTTP 200 / ready，六个插件在线；`.env` 和锁文件保持不变。启动器确认六个安装内容未变化，后续开发同步保持 Server 运行。脚本的 JSON 文本前缀脱敏补充由新增回归及恢复后的启动入口验证，不改变上述验收二进制。

验收二进制指纹：

- Server：`3BB7CBA13EA154AAC21BD5A2E1BD85F6BE1A126F96BE1E0D553DB18C0C7BEA5F`
- subscription-hub：`7E25CD3B468AEFB09B7B89BB32DC75415685346C593D7FE26D2AC3804CFEA7F1`

仍需区分本地修复与外部状态：

- 抖音接口仍有 HTTP 403。服务器凭据复检有效不意味着所有接口都接受请求；没有绕过平台限制，也没有把 403 改写成 CK 失效。
- 历史真实出站中的 9 次回执超时、1 次媒体文件缺失、1 次限流，与插件内部事件失败是不同类别。本地已修正未知结果语义和文件生命周期，但不能保证上游以后必然返回回执。
- 未经证明的原生窗口错误没有归因或宣称修复；不能仅凭 `STATUS_CONTROL_C_EXIT` 判定插件崩溃。
- 本次完成本地开发产物修复，不等同于已发布新的 SDK 或插件版本。外部分发需另行走正式发布流程。
