# Plugin SDK Docs

本目录承载 RayleaBot 官方 SDK 说明，包括：

- Python / Node.js SDK 使用方式。
- 上下文对象。
- 装饰器 / 注册器。
- 能力调用示例。

规则：

- SDK 说明必须服从正式插件协议契约。
- 若 SDK 需要新增字段或消息类型，必须先更新 `contracts/`。
- SDK 只是协议的实现便利层，不得成为独立裁决来源。
