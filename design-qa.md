# 配置工作台占位提示与浮动操作验证

final result: passed

## 比较对象

- 视觉目标：选定的分类工作台及收紧宽度后的设计图，`C:/Users/Raylea/.codex/generated_images/01a07141-f217-75f3-a639-cb9742f8739a/exec-8389272d-0515-4976-8331-1a6bafd4e1d5.png`。后续要求在分类前添加图标，以浮标替代保存栏，未保存时使用警示色；最新调整将默认值移入占位文本，并将撤销入口放在保存浮标左侧；标注参考为 `C:/Users/Raylea/AppData/Local/Temp/codex-clipboard-92827309-7041-40e6-acd7-95b13716edca.png`。
- 本地实现：`http://127.0.0.1:4180/config`，连接测试后端，通过正式登录接口建立演示会话。
- 比较基线：`C:/Users/Raylea/.codex/visualizations/2026/09/05/01a07141-f217-75f3-a639-cb9742f8739a/config-fab-dirty.png`。
- 最终桌面截图：同目录 `config-placeholders-desktop.png`、`config-placeholders-empty.png`；最终窄屏截图：`config-placeholders-mobile-dark.png`。
- 桌面目标和最终截图均为 1672 × 941 像素；实现视口为 1672 × 941 CSS px，deviceScaleFactor 为 1，无缩放归一化。窄屏为 390 × 844 CSS px / 像素，deviceScaleFactor 为 1。
- 桌面比较状态：亮色、图片生成分类、性能与浏览器收起、精度草稿 200%、一项未保存。补充桌面与窄屏暗色的空输入状态，精度与超时仅显示 100、30 的占位文本，实际输入值均为空。用户标注图为裁切后的 1837 × 1441 像素参考，不以它与完整页面的像素比例判断字号或布局误差。

## 发现与处理

首轮发现一项 P2：桌面端同时出现分类导航和窄屏选择器，重复占用导航空间。原因是组件把 class 透传至内部触发按钮，未隐藏外层。改由页面容器控制显示，最终截图只保留当前断点对应的分类入口。

浏览器交互检查补齐了三个行为问题：选择器正确关联行内说明；无搜索结果时仍显示保存错误；离开确认取消正在进行的页面快照，并处理被跳过的快照承诺，避免等待确认时转场超时。确认放弃会清除缓存页面的本地草稿。

悬浮保存迭代的首轮发现一项 P2：未保存时出现的文字撤销按钮在手机上新增一行，推动全部字段下移。通过将撤销入口移出普通布局流解决；最终入口按最新要求位于保存浮标左侧，不占用页头空间或推动正文。

提交前审查另外修复了两项 P2：未完成的 IPC 限流输入原先不会进入草稿，切换分类会恢复旧值；现在保留部分输入并标记未保存，搜索、分类切换和撤销均有回归覆盖。20 个会话、浏览器及运行参数原先缺少重启提示；现在按正式 x-apply-policy 补齐，测试后端的运行参数和时区分类也同步校正。严格契约门禁另补齐了此前账户修改接口的路径登记。复核未发现尚待处理的 P0、P1 或 P2 问题。

## 视觉比较

分类工作台首轮合并查看目标图、桌面和窄屏截图。悬浮保存迭代再次批量检查桌面空闲、未保存和手机暗色，完成上述移动端修正后，将原工作台截图与相同视口、相同编辑状态的最终截图放在同一次比较输入中确认。最新一轮将调整前后的同尺寸、同分类、同草稿状态截图共同比较，再补充清空输入后的桌面与手机状态；未发现新增的 P0/P1/P2 问题。最终截图等待字体加载，并禁用尚未结束的过渡后捕获。

- 字体与层级：沿用产品的 Noto Sans SC，分类标题 20px、字段名称 14px、说明 13px。保留产品的紧凑字号，未照搬生成图较大的标题。名称、说明、控件及保存状态可区分，长说明自然换行。
- 布局节奏：页面宽度上限 1120px，左右居中；200px 分类列、24px 间距和单一编辑表面保持不变。保存操作改为右下方的独立 56px 按钮，圆角 18px，不再绘制整条底栏。保存浮标左侧为 48px 中性撤销按钮，两者间距 12px、垂直居中；保存按钮位置保持稳定。编辑区较生成图宽，采用已确定的 1120px 总宽上限以容纳真实说明和复合控件；宽屏不继续拉伸。
- 颜色与视觉变量：空闲浮标使用中性表面；未保存时使用现有 attention 暖色，配合数量角标提供不依赖颜色的状态信息。亮色按钮为 rgb(155, 74, 47)，前景白色；暗色按钮为 rgb(224, 138, 97)，前景 rgb(30, 30, 30)。失败使用 danger 与错误图标，成功恢复中性。焦点指示保持在按钮内部。
- 图片与图标：本页没有新增插画或位图需求。保留项目已有品牌标志；分类使用盾牌、图片、用户、处理器、数据库和日志图标，统一为现有图标库的 18px 线性样式。保存、撤销、错误与加载同样复用已有组件。
- 文案与内容：六个任务分类保留原有 13 个配置分区的全部字段；字段范围、说明、单位和实际值来自现有定义与正式配置响应。47 个字段的默认值与正式配置 schema 对照，31 处“默认：…”从说明移入占位提示；HTTP 最大重试次数的旧文字 2 校正为正式默认值 1。原有示例占位文本保留，空值占位不会回填或提交。没有复制生成图的示例事实。搜索、未保存项数、重启结果和错误反馈均来自当前状态。

完整截图中的字段名称、分类图标、控件边界与保存浮标已清晰可辨，逐项检查了上述区域，无需额外裁切比较。全局导航按现有产品保留，生成图中不同的导航排序和装饰不是本次修改范围。

## 验证与边界

- 42 项相关单测通过：字段归属、默认值与契约一致、文本／数字／列表／拆分限流的占位提示不回填、草稿保留、保存恢复，以及限流和全局插件设置的共享字段行为。
- 13 项浏览器用例通过：分类键盘操作、高级搜索、跨分类统一提交与回读、继续编辑和确认放弃、失败重试、清空后的默认占位、左撤销右保存及撤销后的焦点归还、未完成限流输入跨分类和搜索保留、亮暗主题下浮标的空闲／未保存／保存中／恢复状态、响应式布局、移动触控、既有 IPC 限流和抖音浏览器设置保存，以及相关帮助与就绪操作。
- 320、390、768、1024 和 2560px 宽度已检查；窄屏内容无横向溢出，悬浮按钮为 56 × 56px，位于视口内。最终 390px 截图中保存按钮位于 x=318、y=752，撤销按钮位于 x=258、y=756，尺寸分别为 56px 和 48px。复杂字段和长分类通过现有选择器及页面纵向滚动访问。
- 浏览器主流程未见未处理页面错误。TypeScript 与 Vite 生产构建、契约严格校验及校验器自测通过，预期配置页面 bundle 已生成。
- 本轮使用测试后端验证浏览器流程，未向实际运行服务写入配置；未运行完整 Web 测试集或原生 Launcher 集成。

## 交付检查

- [x] 分类图标、常用与高级参数、占位提示、搜索和左右浮动操作可用。
- [x] 草稿、撤销、离开确认、加载及错误状态保留。
- [x] 桌面与窄屏完成一次批量检查和一次修正后确认。
- [x] 设计说明与管理面用户说明同步。

后续没有阻塞交付的视觉待办。
