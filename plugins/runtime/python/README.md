# RayleaBot Python plugin runtime

`rayleabot-plugin-runtime` provides the production client used by Python plugins to communicate with RayleaBot.

Plugins should declare an exact runtime dependency in `info.json` and import `rayleabot_runtime` directly.
