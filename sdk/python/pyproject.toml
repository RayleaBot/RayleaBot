[project]
name = "rayleabot-sdk"
version = "0.1.0"
description = "RayleaBot Python plugin development kit"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["rayleabot-plugin-runtime==0.1.0"]
license = "AGPL-3.0-only"
license-files = ["LICENSE"]

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
