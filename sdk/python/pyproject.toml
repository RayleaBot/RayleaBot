[project]
name = "rayleabot-sdk"
version = "0.1.0"
description = "RayleaBot Plugin SDK for Python"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends._legacy:_Backend"
