# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

RayleaBot 管理面面向个人开发者、自用机器人部署者和开源协作者。操作者通常在桌面端持续查看系统状态、调整插件与治理配置、处理恢复事项，并需要在不中断日常运维节奏的情况下完成管理操作。

## Product Purpose

RayleaBot 为自托管聊天机器人提供清晰、可验证、可恢复的本地管理体验。界面应帮助操作者快速判断系统是否健康、下一步是否需要人工处理，以及操作完成后正式状态是否已经生效。

## Positioning

RayleaBot 以 contract 驱动的本地控制面连接 OneBot11、插件运行时、Web 管理面和桌面启动器。服务端持有正式状态，其他界面只展示可丢弃视图，使配置、诊断、长任务和恢复结果能够从同一来源核实。

## Operating Context

RayleaBot 由个人开发者或开源协作者部署在自有 Windows、macOS 或 Linux 环境中。操作者通过 Web 管理面处理日常配置与治理，通过 Launcher 完成本机预检和服务进程编排，并在服务停机窗口使用 CLI 执行维护、备份与恢复。插件以受信本机进程运行，长期服务需要明确暴露降级、人工处理和恢复事项。

插件管理保留独立对象卡片，便于同时辨认身份、状态并直接操作，不以紧凑数据行替代。2K 桌面以每屏约 10–15 张完整卡片为密度目标，实际数量受内容、视口和宽度偏好影响；卡片操作使用带可访问名称与提示的图标，指令与别名通过概要或详情查看。

## Capabilities and Constraints

- 当前正式聊天协议是 OneBot11，提供 `reverse_ws`、`forward_ws`、`http_api` 和 `webhook` 四种传输。
- 正式部署模型是单实例、本地 SQLite 和单活跃 OneBot；Web 与 Launcher 不持有第二份业务状态。
- 插件以经过 artifact 校验的本机原生代码运行，通过 manifest permissions 和 JSONL 协议访问平台能力；permissions 不是操作系统沙盒。
- 对外字段、状态、错误码、事件、CLI 和发布元数据以 `contracts/` 为唯一正式来源。
- 配置、业务数据和已安装插件需要支持可验证备份、恢复与版本升级，不以云端控制面作为运行前提。

## Brand Commitments

安静、精密、可信。界面保持优雅但不追求装饰性惊喜，熟悉的交互和稳定的状态反馈应让工具退到任务之后。

登录、Web 管理面与 Windows Launcher 使用“雾白·青瓷”：中性灰白或炭灰表面、少量青瓷强调与简约几何折叶标识。青瓷用于品牌、主操作和少数选中标记，普通背景、边框和文字保持中性。高信息密度依靠紧凑排版、对齐与必要分隔建立；亮暗主题保留等价的状态和操作能力。

## Anti-references

- 通用的深色科技感仪表盘，把霓虹蓝、发光边框和装饰图表当作专业感。
- 强烈渐变、渐变文字、全页绿色染色和无意义装饰；玻璃仅用于具有不透明降级的少量浮层，表单、列表和日志使用实底。
- 无任务意义的同尺寸卡片墙、嵌套卡片和组件拼贴式页面；独立插件集合明确保留卡片，不将这一例外推广到所有数据页。
- hero 指标模板，用巨型数字、辅助统计和渐变强调代替真实任务层级。
- 巨型页面标题、装饰性眉题、经典机器人吉祥物和为填满布局而虚构的数据。
- 彩色侧边条、装饰性动效和依赖颜色单独表达状态。
- 为了风格重造标准控件，导致 Web、Launcher 和插件页面出现互不一致的操作词汇。

## Evidence on Hand

- 产品范围与入口：`README.md`、`docs/RayleaBot机器人项目规划.md`
- 正式接口与状态语义：`contracts/`、`docs/architecture/`
- 视觉语言与机器 token：`DESIGN.md`、`design/tokens.json`、`.impeccable/design.json`
- 当前实现证据：`web/src/`、`launcher/src/`、`server/internal/`
- 产品规范不提供用户研究、客户证言、市场排名或稳定帧率承诺；性能与设备兼容性以对应环境的可复现实测为准。

## Product Principles

- 本地所有权：运行、数据、凭据和恢复路径由部署者控制。
- 单一正式状态：每项业务状态都能追溯到服务端或正式 contract，不由客户端猜测重建。
- 可核实操作：长任务、配置生效、诊断和恢复都提供明确结果与下一步。
- 连续运行优先：管理操作、插件隔离和失败边界不得无故打断其他健康能力。
- 受控扩展：插件复用统一协议、能力网关和安装事务，不建立平行平台栈。

## Accessibility & Inclusion

- Web、Launcher 和官方插件页面以 WCAG 2.2 AA 为无障碍验收目标。
- 所有正式操作可通过键盘完成，并提供清晰、持续可见的焦点状态。
- 状态不能只依赖颜色区分，必须同时提供文字、图标或结构化标签。
- 尊重 `prefers-reduced-motion`，移除非必要运动并保留即时状态反馈。
- reduced-transparency、forced-colors 或浮层模糊不可用时，使用不透明表面保持可读与可操作。
- 在缩放、窄屏、粗指针和中英文混排场景中保持内容可读、控件可达和操作完整。
