{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rayleabot.local/contracts/backup-manifest.schema.json",
  "title": "RayleaBot Backup Manifest",
  "description": "Formal schema for backup-manifest.json. This file freezes the packaged recovery preflight surface used by backup, restore, launcher fallback, and cross-version recovery drills.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version",
    "created_at",
    "core_version",
    "config_schema_version",
    "db_schema_version",
    "plugin_manifest_version",
    "plugin_protocol_version",
    "plugin_artifact_version",
    "plugin_ui_bridge_version",
    "consistency",
    "directories"
  ],
  "properties": {
    "version": {
      "type": "string",
      "const": "3"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "core_version": {
      "$ref": "#/$defs/semver"
    },
    "config_schema_version": {
      "type": "string",
      "minLength": 1
    },
    "db_schema_version": {
      "type": "string",
      "minLength": 1
    },
    "plugin_manifest_version": {
      "type": "string",
      "const": "3"
    },
    "plugin_protocol_version": {
      "type": "string",
      "const": "2"
    },
    "plugin_artifact_version": {
      "type": "string",
      "const": "2"
    },
    "plugin_ui_bridge_version": {
      "type": "string",
      "const": "3"
    },
    "consistency": {
      "type": "string",
      "enum": ["offline", "online"]
    },
    "plugins": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/plugin"
      },
      "uniqueItems": true
    },
    "directories": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/directory"
      },
      "minItems": 1
    }
  },
  "$defs": {
    "semver": {
      "type": "string",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"
    },
    "directory": {
      "type": "object",
      "description": "A restorable archive root. config contains the effective user configuration, database identifies the canonical SQLite snapshot, data contains non-database runtime and plugin business data, and plugins contains installed plugin artifacts.",
      "additionalProperties": false,
      "required": ["label", "path"],
      "properties": {
        "label": {
          "type": "string",
          "enum": ["config", "database", "data", "plugins"]
        },
        "path": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "plugin": {
      "type": "object",
      "additionalProperties": false,
      "required": ["plugin_id", "manifest_version", "protocol_version", "artifact_version"],
      "properties": {
        "plugin_id": {
          "type": "string",
          "pattern": "^[a-z0-9](?:[a-z0-9._-]{0,62}[a-z0-9])?$"
        },
        "manifest_version": {
          "type": "string",
          "enum": ["2", "3"],
          "description": "Actual manifest version found in the archived package, including preserved unsupported v2 packages."
        },
        "protocol_version": {
          "type": "string",
          "enum": ["1", "2"],
          "description": "Actual protocol version found in the archived package."
        },
        "artifact_version": {
          "type": "string",
          "enum": ["1", "2"],
          "description": "Actual artifact version found in the archived package."
        },
        "version": {
          "$ref": "#/$defs/semver"
        },
        "min_core_version": {
          "$ref": "#/$defs/semver"
        },
        "source_root": {
          "type": "string",
          "enum": ["plugins/installed"]
        }
      }
    }
  },
  "x-fixtures": [
    "fixtures/backup-manifest/ok.minimal.json",
    "fixtures/backup-manifest/invalid.legacy-v2.json",
    "fixtures/backup-manifest/invalid.missing-schema-versions.json",
    "fixtures/backup-manifest/edge.incompatible-plugin-inventory.json"
  ]
}
