{
  "contract": "contracts/plugin-artifact.schema.json",
  "case": "edge",
  "input": {
    "artifact_version": "2",
    "target_platform": "macos-arm64",
    "entry": "bin/fortune"
  },
  "expect": {
    "valid": true,
    "notes": [
      "The installer discovers and validates info.json, the native entry, UI files and templates from the extracted package."
    ]
  }
}
