contract: contracts/cli-commands.yaml#commands.plugin.subcommands.dev-sync
case: ok
input:
  command: raylea plugin dev-sync --artifact .tmp/plugin-dev/windows-x64/raylea.echo --source ../RayleaBotPlugins/plugin-echo
  mode: offline
expect:
  valid: true
  exit_code: 0
  task_type: null
  notes:
    - The standard startup script builds the artifact before invoking this command.
    - Existing development installations are atomically replaced through the same installer transaction.
