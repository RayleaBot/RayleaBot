contract: contracts/cli-commands.yaml#commands.restore
input:
  command: restore
  mode: offline
expect:
  valid: true
  exit_code: 0
  task_model: true
  task_type: restore.apply
  recovery_summary_status: pending
