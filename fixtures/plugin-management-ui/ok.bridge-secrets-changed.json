{
  "contract": "contracts/plugin-management-ui-bridge.schema.json",
  "case": "ok",
  "description": "The host returns plugin-owned secrets after reload or save.",
  "input": {
    "version": "1",
    "source": "management_host",
    "type": "secrets.changed",
    "request_id": "req_plugin_secrets_changed_0001",
    "payload": {
      "values": {
        "bili_token_primary": "SESSDATA=fixture; bili_jct=fixture"
      },
      "changed_keys": [
        "bili_token_primary"
      ]
    }
  },
  "expect": {
    "valid": true
  }
}
