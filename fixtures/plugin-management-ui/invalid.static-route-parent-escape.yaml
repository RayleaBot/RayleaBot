contract: contracts/plugin-management-ui.yaml
case: invalid
description: Static route requests cannot escape the artifact UI root.
request:
  method: GET
  host: p-0123456789abcdef.plugins.localhost:8080
  path: /../bin/example-config-panel
response:
  status: 404
expect:
  valid: true
