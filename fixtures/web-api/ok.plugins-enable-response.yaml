contract: contracts/web-api.openapi.yaml
case: ok
description: Enable is synchronous and only updates desired_state.
request:
  method: POST
  path: /api/plugins/weather/enable
response:
  status: 200
  body:
    plugin:
      id: weather
      name: Weather
      role: user
      registration_state: installed
      desired_state: enabled
      runtime_state: starting
      display_state: enabling
      source:
        root: plugins/installed
        package_source_type: local_zip
        package_source_ref: C:/plugins/weather.zip
        verified: false
      trust:
        level: unverified
        label: 未验证来源
      commands:
        - name: weather
          aliases:
            - tq
            - 天气
          description: 查询天气
          usage: weather <城市>
          permission: member
          command_source: manifest
      command_conflicts: []
      permissions:
        - capability: http.request
          requirement: required
          status: granted
          source: persisted
          expires_at: null
        - capability: logger.write
          requirement: optional
          status: granted
          source: config_auto
          expires_at: null
expect:
  valid: true
  notes:
    - No task_id is returned.
    - runtime_state is allowed to lag behind desired_state.
