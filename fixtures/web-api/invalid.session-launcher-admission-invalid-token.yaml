contract: contracts/web-api.openapi.yaml
case: invalid
description: Launcher admission rejects unknown, expired, or already-consumed launcher tokens.
request:
  method: POST
  path: /api/session/launcher-admission
  body:
    launcher_token: launcher_token_invalid_fixture_0001
response:
  status: 401
  body:
    error:
      code: permission.denied
      message: 当前用户无权执行该操作
      message_key: errors.permission.denied
      request_id: req_launcher_admission_denied_0001
expect:
  valid: true
