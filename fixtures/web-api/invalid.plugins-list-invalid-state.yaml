contract: contracts/web-api.openapi.yaml
case: invalid
description: Plugin list rejects state values outside the formal enum.
request:
  method: GET
  path: /api/plugins
response:
  status: 200
  body:
    items:
    - id: weather
      name: Weather
      role: community
      state: paused
      commands: []
      command_groups: []
      help: {}
    total: 1
expect:
  valid: false
  reason: state must stay within the formal PluginState enum.
