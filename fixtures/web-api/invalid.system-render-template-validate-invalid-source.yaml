contract: contracts/web-api.openapi.yaml
case: invalid
description: Template validation rejects draft source bundles that fail formal source checks.
request:
  method: POST
  path: /api/system/render/templates/help.menu/validate
  body:
    source:
      manifest_json:
        version: '1'
      html: '<section></section>'
      stylesheet: '.menu-card {}'
      input_schema_json: null
response:
  status: 400
  body:
    error:
      code: platform.template_source_invalid
      message_key: errors.platform.template_source_invalid
expect:
  valid: true
