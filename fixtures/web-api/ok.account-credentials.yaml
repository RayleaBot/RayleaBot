contract: contracts/web-api.openapi.yaml
case: ok
description: Changing the password and username atomically invalidates all existing sessions.
request:
  method: PUT
  path: /api/account/credentials
  body:
    current_secret: fixture-only-old-password
    new_secret: fixture-only-new-password
    new_identifier: new-admin
response:
  status: 204
expect:
  valid: true
