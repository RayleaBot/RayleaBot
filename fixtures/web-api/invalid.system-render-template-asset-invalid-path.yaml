contract: contracts/web-api.openapi.yaml
case: invalid
description: Template live preview rejects resources outside the controlled template root.
request:
  method: GET
  path: /api/system/render/templates/help.menu/asset?path=../../config/user.yaml
response:
  status: 404
  body:
    error:
      code: platform.resource_not_found
      message: "请求的资源不存在"
      message_key: errors.platform.resource_not_found
      request_id: req_render_asset_invalid_path
      details:
        resource_type: render_template_asset
expect:
  valid: true
