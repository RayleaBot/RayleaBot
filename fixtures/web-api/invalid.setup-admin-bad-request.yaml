contract: contracts/web-api.openapi.yaml
case: invalid
description: Bootstrap rejects malformed requests without exposing setup internals.
request:
  method: POST
  path: /api/setup/admin
  body:
    identifier: admin
response:
  status: 400
  body:
    error:
      code: platform.invalid_request
      message: 请求参数不合法
      message_key: errors.platform.invalid_request
      request_id: req_setup_admin_invalid_0001
expect:
  valid: true
