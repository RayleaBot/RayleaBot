contract: contracts/web-api.openapi.yaml
case: ok
description: Tasks list returns the latest matching task snapshots.
request:
  method: GET
  path: /api/tasks?status=running&task_type=plugin.install&limit=1
response:
  status: 200
  body:
    items:
      - task_id: task_plugin_install_0001
        task_type: plugin.install
        status: running
        progress: 25
        summary: install weather
expect:
  valid: true
