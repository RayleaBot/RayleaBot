contract: contracts/web-api.openapi.yaml
case: invalid
description: Uninstall is rejected when plugin_id does not exist.
request:
  method: DELETE
  path: /api/plugins/nonexistent
response:
  status: 404
  body:
    error:
      code: platform.resource_not_found
      message: "请求的资源不存在"
      message_key: errors.platform.resource_not_found
      request_id: req_001
      details:
        resource_type: plugin
        plugin_id: nonexistent
expect:
  valid: true
