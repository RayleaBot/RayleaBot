contract: contracts/web-api.openapi.yaml
case: edge
description: Disable is synchronous and may return while the runtime is still stopping.
request:
  method: POST
  path: /api/plugins/weather/disable
response:
  status: 200
  body:
    plugin:
      id: weather
      name: Weather
      role: community
      state: stopping
      source:
        root: plugins/installed
        package_source_type: local_zip
        package_source_ref: C:/plugins/weather.zip
        verified: false
      trust:
        level: unverified
        label: 未验证来源
      permissions:
        message.send: true
      webhooks: []
      commands:
        - id: weather
          name: 天气查询
          effective_names: [weather, tq, 天气]
          description: 查询天气
          usage: weather <城市>
          permission: everyone
          trigger:
            type: exact
            names: [weather, tq, 天气]
      command_groups:
        - id: query
          title: 查询
          commands: [weather]
      help: {}
      command_conflicts: []
expect:
  valid: true
