contract: contracts/web-api.openapi.yaml
case: invalid
description: Template rollback rejects invalid rollback targets even when the revision exists.
request:
  method: POST
  path: /api/system/render/templates/help.menu/rollback
  body:
    target_revision_id: rev_help_menu_0005
    base_revision_id: rev_help_menu_0005
    message: 尝试回退到当前版本
response:
  status: 409
  body:
    error:
      code: platform.template_rollback_target_invalid
      message_key: errors.platform.template_rollback_target_invalid
expect:
  valid: true
