contract: contracts/web-api.openapi.yaml
case: edge
description: Omitting the optional username keeps the current identifier when changing the password.
request:
  method: PUT
  path: /api/account/credentials
  body:
    current_secret: fixture-only-old-password
    new_secret: fixture-only-new-password
response:
  status: 204
expect:
  valid: true
