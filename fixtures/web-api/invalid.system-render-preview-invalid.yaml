contract: contracts/web-api.openapi.yaml
case: invalid
description: Management render preview is rejected when the request body misses required fields.
request:
  method: POST
  path: /api/system/render/preview
  body:
    theme: default
    output: png
    data:
      title: 帮助菜单
response:
  status: 400
  body:
    error:
      code: platform.invalid_request
      message: 请求参数不合法
      message_key: errors.platform.invalid_request
      request_id: req_render_preview_invalid
expect:
  valid: true
