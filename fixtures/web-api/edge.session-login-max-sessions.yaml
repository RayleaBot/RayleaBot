contract: contracts/web-api.openapi.yaml
case: edge
description: Login recycles the oldest active management session when the configured max_sessions boundary is already reached.
request:
  method: POST
  path: /api/session/login
  body:
    identifier: admin
    secret: fixture-only-secret
response:
  status: 200
  body:
    transport: bearer
    session_token: fixture_session_token_placeholder
    expires_at: "2026-03-26T10:00:00Z"
expect:
  valid: true
