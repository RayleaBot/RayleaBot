contract: contracts/web-api.openapi.yaml
case: edge
description: Login is conservatively rejected when the configured max_sessions boundary is already reached.
request:
  method: POST
  path: /api/session/login
  body:
    identifier: admin
    secret: fixture-only-secret
response:
  status: 403
  body:
    error:
      code: permission.denied
      message: 当前用户无权执行该操作
      message_key: errors.permission.denied
      request_id: req_session_login_denied_0002
expect:
  valid: true
