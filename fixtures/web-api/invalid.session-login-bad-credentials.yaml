contract: contracts/web-api.openapi.yaml
case: invalid
description: Bad management credentials are rejected without leaking account lookup details.
request:
  method: POST
  path: /api/session/login
  body:
    identifier: admin
    secret: wrong-secret
response:
  status: 403
  body:
    error:
      code: permission.denied
      message: 当前用户无权执行该操作
      message_key: errors.permission.denied
      request_id: req_session_login_denied_0001
expect:
  valid: true
