contract: contracts/web-api.openapi.yaml
case: invalid
description: Template rollback rejects revision identifiers that do not exist.
request:
  method: POST
  path: /api/system/render/templates/help.menu/rollback
  body:
    target_revision_id: rev_help_menu_missing
    base_revision_id: rev_help_menu_0005
    message: 恢复到缺失版本
response:
  status: 404
  body:
    error:
      code: platform.template_revision_not_found
      message_key: errors.platform.template_revision_not_found
expect:
  valid: true
