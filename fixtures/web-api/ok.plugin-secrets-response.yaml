contract: contracts/web-api.openapi.yaml
case: ok
description: Plugin secret reads return plugin-owned plaintext values to an authenticated management page.
request:
  method: GET
  path: /api/plugins/subscription-hub/secrets
response:
  status: 200
  body:
    plugin_id: subscription-hub
    values:
      bili_token_primary: "SESSDATA=fixture; bili_jct=fixture"
      bili_token_backup: "SESSDATA=backup; bili_jct=backup"
expect:
  valid: true
