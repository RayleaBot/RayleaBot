contract: contracts/web-api.openapi.yaml
case: ok
description: Plugin secret reads expose configured state without returning plaintext.
request:
  method: GET
  path: /api/plugins/subscription-hub/secrets
response:
  status: 200
  body:
    plugin_id: subscription-hub
    configured:
      bili_token_primary: true
      bili_token_backup: false
expect:
  valid: true
