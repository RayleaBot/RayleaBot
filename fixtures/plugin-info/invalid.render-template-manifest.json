{
  "contract": "contracts/plugin-info.schema.json",
  "case": "invalid",
  "description": "A declared render template directory must contain a valid template manifest.",
  "input": {
    "id": "weather-card",
    "name": "Weather Card",
    "version": "1.0.0",
    "manifest_version": "2",
    "plugin_protocol_version": "1",
    "runtime": "go",
    "entry": "bin/weather-card",
    "license": "MIT",
    "description": "Weather plugin with an invalid local render card manifest.",
    "author": "raylea",
    "capabilities": [
      "event.subscribe",
      "render.image"
    ],
    "render_templates": [
      {
        "path": "templates/card"
      }
    ],
    "platforms": [
      "windows-x64",
      "linux-x64",
      "macos-arm64"
    ]
  },
  "package_files": {
    "templates/card/template.json": "not-json"
  },
  "expect": {
    "valid": false,
    "reason": "install-time validation rejects declared template directories whose template.json cannot be compiled as a render template."
  }
}
