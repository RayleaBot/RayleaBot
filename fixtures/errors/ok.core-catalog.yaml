contract: contracts/error-codes.yaml
case: ok
description: Core catalog excerpt with required fields and applicability metadata.
input:
  codes:
    - code: platform.task_not_cancellable
      message_key: errors.platform.task_not_cancellable
      message: 当前任务不可取消
      description: The task has already entered an irreversible stage.
      http_status: 409
      retryable: false
      applies_to: [http, websocket, task]
    - code: plugin.capability_violation
      message_key: errors.plugin.capability_violation
      message: 插件访问未声明能力
      description: The plugin used a capability that is absent from its manifest declaration or outside its declared capability parameters.
      http_status: 403
      retryable: false
      applies_to: [http, websocket, plugin_protocol, task]
expect:
  valid: true
