contract: contracts/plugin-protocol.schema.json
case: ok
frames:
  - protocol_version: "1"
    type: action
    timestamp: 1710000431
    plugin_id: subscription-hub
    request_id: local_secret_0001
    parent_request_id: event_poll_0001
    action: secret.read
    data:
      key: bili_token_primary
expect:
  valid: true
  notes:
    - secret.read reads one key from the caller plugin secret namespace.
