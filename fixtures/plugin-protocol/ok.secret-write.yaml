contract: contracts/plugin-protocol.schema.json
case: ok
frames:
  - type: action
    request_id: action-secret-write-1
    action: secret.write
    data:
      values:
        account.example.cookie: fixture-only-secret
  - type: result
    request_id: action-secret-write-1
    status: success
    data:
      changed_keys:
        - account.example.cookie
expect:
  valid: true
