contract: contracts/plugin-protocol.schema.json
case: ok
frames:
  - protocol_version: "1"
    type: action
    timestamp: 1710000411
    plugin_id: raylea.help
    request_id: local_plugin_list_0001
    parent_request_id: event_help_0001
    action: plugin.list
    data: {}
expect:
  valid: true
  notes:
    - plugin.list reads the current installed plugin catalog through the local action channel.
