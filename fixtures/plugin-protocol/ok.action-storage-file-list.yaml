contract: contracts/plugin-protocol.schema.json
case: ok
frames:
  - protocol_version: "1"
    type: action
    timestamp: 1710000305
    plugin_id: scope-cache
    request_id: local_file_0005
    action: storage.file
    data:
      operation: list
      root: plugin_data
      prefix: cache/
expect:
  valid: true
  notes:
    - storage.file.list returns slash-normalized relative paths under one prefix.
