contract: contracts/plugin-protocol.schema.json
case: edge
frames:
  - protocol_version: "1"
    type: action
    timestamp: 1710000441
    plugin_id: subscription-hub
    request_id: local_secret_result_0001
    parent_request_id: event_poll_0001
    action: secret.read
    data:
      key: bili_token_primary
  - protocol_version: "1"
    type: result
    timestamp: 1710000442
    plugin_id: subscription-hub
    request_id: local_secret_result_0001
    status: success
    data:
      key: bili_token_primary
      exists: true
      value: "SESSDATA=fixture; bili_jct=fixture"
expect:
  valid: true
  notes:
    - secret.read result values are visible only to the requesting plugin process.
