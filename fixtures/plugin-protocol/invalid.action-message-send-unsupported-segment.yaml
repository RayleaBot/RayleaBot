contract: contracts/plugin-protocol.schema.json
case: invalid
frames:
  - protocol_version: "1"
    type: action
    timestamp: 1710000104
    plugin_id: weather
    request_id: act_invalid_0001
    action: message.send
    data:
      target_type: group
      target_id: "456"
      message:
        segments:
          - type: markdown
            data:
              text: "# unsupported"
expect:
  valid: false
  reason: outbound message segments must stay within the frozen segment enum.
