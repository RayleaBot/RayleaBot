contract: contracts/plugin-protocol.schema.json
case: invalid
frames:
  - protocol_version: "1"
    type: action
    timestamp: 1710000451
    plugin_id: subscription-hub
    request_id: local_secret_invalid_0001
    parent_request_id: event_poll_0001
    action: secret.read
    data:
      key: ""
expect:
  valid: false
  reason: secret.read requires a non-empty key.
