contract: contracts/plugin-protocol.schema.json
case: invalid
frames:
  - protocol_version: "1"
    type: action
    timestamp: 1710000002
    plugin_id: weather
    request_id: req_0001
    action: group.kick
    data:
      target_type: group
      target_id: "456"
      user_id: "789"
expect:
  valid: false
  reason: only message.send, message.reply, logger.write, storage.kv, storage.file, and http.request are frozen action kinds; other action kinds remain out of scope.
