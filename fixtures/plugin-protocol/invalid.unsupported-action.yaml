contract: contracts/plugin-protocol.schema.json
case: invalid
frames:
  - protocol_version: "1"
    type: action
    timestamp: 1710000002
    plugin_id: weather
    request_id: req_0001
    action: message.reply
    data:
      target_type: group
      target_id: "456"
      text: reply text
expect:
  valid: false
  reason: only the single outbound action action=message.send is frozen; other action kinds remain out of scope.
