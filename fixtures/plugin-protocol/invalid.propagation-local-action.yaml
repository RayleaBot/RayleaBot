contract: contracts/plugin-protocol.schema.json
case: invalid
frames:
- type: action
  request_id: action-1
  action: message.send
  data:
    target_type: group
    target_id: '20001'
    message:
      segments:
      - type: text
        data:
          text: fixture
  propagation: continue
  parent_request_id: event-1
expect:
  valid: false
