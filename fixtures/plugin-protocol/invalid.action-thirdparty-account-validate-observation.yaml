contract: contracts/plugin-protocol.schema.json
case: invalid
frames:
  - protocol_version: "1"
    type: action
    timestamp: 1710000454
    plugin_id: subscription-hub
    request_id: local_thirdparty_account_validate_invalid_0001
    parent_request_id: event_subscription_check_0001
    action: thirdparty.account.validate
    data:
      platform: weibo
      account_id: primary
      observation: cookie_expired
expect:
  valid: false
  reason: thirdparty.account.validate accepts only bounded authentication observations.
