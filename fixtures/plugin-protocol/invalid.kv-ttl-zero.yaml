contract: contracts/plugin-protocol.schema.json
case: invalid
frames:
- type: action
  request_id: kv-fixture
  parent_request_id: event-fixture
  action: storage.kv
  data:
    operation: set
    key: dedup
    value: 0
    ttl_seconds: 0
expect:
  valid: false
