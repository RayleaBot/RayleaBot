contract: contracts/plugin-protocol.schema.json
case: ok
frames:
  - type: action
    request_id: action-secret-delete-1
    action: secret.delete
    data:
      keys:
        - account.example.cookie
  - type: result
    request_id: action-secret-delete-1
    status: success
    data:
      changed_keys:
        - account.example.cookie
expect:
  valid: true
