contract: contracts/plugin-protocol.schema.json
case: ok
frames:
  - protocol_version: "1"
    type: action
    timestamp: 1710000200
    plugin_id: notice-logger
    request_id: local_0001
    action: logger.write
    data:
      level: info
      message: member notice received
      fields:
        event_type: notice.member_increase
        actor_id: "3001"
expect:
  valid: true
  notes:
    - logger.write is a platform-local action that uses a distinct request_id inside event handling.
    - The platform answers this request_id with result or error before the plugin finishes the outer event.
